import { PythonProvidedStaticCodeBlock } from '@/components/python-provided'
import { ServerPythonProvidedStaticCodeBlock } from '@/components/server-python-provided'


# Welcome to my MDX page!
 
This is some **bold** and _italics_ text.
 
This is a list in markdown:
 
- One
- Two
- Three
 
Checkout my React component:

A code block:

```python
x = 1
print(x)
```

## Static Python Block

<PythonProvidedStaticCodeBlock code={
    `
    print('Hello, World!')
    `} />


## Server Python Block (runs on client but rendering entrypoint is server)

<ServerPythonProvidedStaticCodeBlock code={
    `
    import time
    for i in range(5):
        time.sleep(1)
        print(f'Hello, World, from the server! {i}')
    `} />